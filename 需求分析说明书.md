# 校园植物认养与养护系统需求分析说明书

## 1. 引言

### 1.1 项目背景
随着高校生态校园建设的深入，校园绿化面积不断扩大。传统的后勤集中管理模式存在人力成本高、师生参与度低、植物养护不及时等问题。本项目旨在开发一套“校园植物认养与养护系统”，通过数字化手段将植物养护与劳动教育深度融合，鼓励学生认养校园植物，负责日常浇水、除草等轻量级养护工作，既减轻了后勤压力，又培养了学生的责任感与劳动意识。

### 1.2 文档目的
本文档旨在明确“校园植物认养与养护系统”的各项功能需求、非功能需求及业务规则，界定系统边界，为后续的架构设计、数据库设计及编码实现提供详尽的依据。本文档完全符合本科软件工程毕业设计规范。

## 2. 总体描述

### 2.1 用户角色分析
系统主要包含三类角色，各角色职责如下：

1.  **普通用户（学生/教职工）**
    *   **权限**：注册登录、浏览植物、申请认养、查看我的认养、执行养护任务（打卡）、上报植物异常、发布/点赞知识分享。
    *   **限制**：**每人限领一株**植物，养护任务超时将受惩罚。

2.  **后勤养护员**
    *   **权限**：发布与维护植物基础信息、处理用户上报的植物异常（病虫害/枯死）、协助审核高难度养护任务。
    *   **职责**：作为专业托底力量，确保植物在未被认养或认养人失职时的存活。

3.  **系统管理员**
    *   **权限**：用户管理、角色权限分配（RBAC）、认养申请审批、成果评比管理、系统参数配置。
    *   **职责**：维持系统正常运行，处理业务纠纷。

## 3. 功能需求分析

本系统包含8大核心功能模块：

### 3.1 用户管理模块
*   **功能描述**：提供用户注册、登录、个人信息维护及密码重置功能。
*   **输入**：学号/工号、密码、手机号。
*   **业务规则**：
    *   注册时需校验学号/工号的唯一性。
    *   密码需进行加密存储（如BCrypt）。

### 3.2 系统管理模块
*   **功能描述**：基于RBAC模型实现角色与权限的动态配置。
*   **功能点**：用户管理、角色管理、菜单管理、权限分配。
*   **输入**：角色名称、权限标识（如 `plant:add`）。
*   **输出**：动态路由表、按钮级权限控制。

### 3.3 植物信息管理
*   **功能描述**：建立校园植物电子档案，支持一植一码。
*   **功能点**：植物录入、编辑、删除、查询、二维码生成。
*   **输入**：植物名称、科属、位置（GPS/区域）、照片URL、当前状态。
*   **业务规则**：
    *   植物状态流转：`待认养` <-> `已认养` <-> `养护中`（异常）。
    *   图片存储采用URL引用，不存Base64。

### 3.4 认养申请管理
*   **功能描述**：处理用户的认养请求及审批流程。
*   **流程**：用户提交申请 -> 管理员审核 -> 结果通知。
*   **业务规则（核心）**：
    *   **限额控制**：系统自动校验该用户当前是否存在状态为“ACTIVE（生效中）”的认养记录。若存在，则直接拦截申请，提示“每人限领一株”。
    *   **联动更新**：审核通过后，自动将对应植物状态更为`已认养`，并生成认养记录。

### 3.5 养护任务管理
*   **功能描述**：生成并追踪养护任务的执行情况。
*   **功能点**：任务发布（系统自动/人工）、任务打卡（上传照片）、任务审核。
*   **业务规则（核心）**：
    *   **超时惩罚**：若任务截止日期（Due Date）已过 **3天** 且状态仍为 `PENDING`，系统自动触发定时任务：
        1.  将该任务标记为 `OVERDUE`。
        2.  **自动取消**该用户的认养资格（认养记录置为 `CANCELLED`）。
        3.  植物状态回滚为 `待认养`。

### 3.6 植物健康监测
*   **功能描述**：监控植物生长状态，处理异常情况。
*   **功能点**：异常上报（用户拍图）、异常处理（后勤接单）、处理反馈。
*   **业务规则（核心）**：
    *   **超时告警**：若异常工单提交后 **48小时** 内，后勤人员未进行“处理”操作（状态未更新），系统自动向管理员发送站内信或邮件进行**二次提醒**。

### 3.7 认养成果与评比
*   **功能描述**：学期末对认养成果进行量化考核。
*   **功能点**：数据统计、评优筛选、证书颁发。
*   **业务规则（核心）**：
    *   **参选资格**：仅当用户在当前学期内的所有养护任务**完成率达到 100%** 时，才自动进入“优秀养护人”候选列表，否则不可参选。

### 3.8 植物知识共享
*   **功能描述**：搭建植物养护经验交流社区。
*   **功能点**：文章发布、列表展示、点赞、评论。
*   **业务规则（核心）**：
    *   **推荐算法**：当单篇知识分享的点赞数 **≥ 20** 时，系统自动为其添加“推荐阅读”标签，并在首页热门区域展示。

## 4. 非功能需求

1.  **性能指标**：
    *   系统平均响应时间 ≤ 500ms。
    *   支持 500+ 用户并发访问，保证系统稳定性。
    *   静态资源（图片）采用CDN加速或独立文件服务器。
2.  **安全性**：
    *   所有敏感操作（如审核、删除）需进行二次鉴权。
    *   防止SQL注入与XSS攻击。
3.  **可靠性**：
    *   数据每日自动备份。
    *   关键业务（如认养状态变更）需保证事务一致性。

## 5. 部署架构要求
*   **Web服务器**：Tomcat 10
*   **数据库**：MySQL 9.0
*   **后端框架**：Spring Boot 3.x
*   **前端架构**：Vue 3 + Axios
