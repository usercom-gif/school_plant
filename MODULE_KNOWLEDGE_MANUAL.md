# 知识分享模块功能说明书

## 1. 模块概述
知识分享模块旨在为用户提供一个交流植物养护经验、分享知识的平台。用户可以发布、查看、点赞、举报帖子，管理员负责审核和管理帖子。

## 2. 功能清单与业务规则

### 2.1 帖子发布与管理
- **发布帖子**：
    - 必填项：标题、内容。
    - 选填项：标签（支持多选，如 #浇水技巧、#施肥知识）。
    - 状态流转：提交后默认进入 `PENDING`（待审核）状态。
- **编辑帖子**：
    - 仅发布者本人可编辑。
    - 仅限 `PENDING` 或 `REJECTED` 状态的帖子可编辑。
    - 编辑后状态重置为 `PENDING`，需重新审核。
- **删除帖子**：
    - 发布者可删除自己的帖子。
    - 管理员可删除任意帖子。

### 2.2 帖子展示与互动
- **列表展示**：
    - 支持按关键词（标题/内容/标签）模糊搜索。
    - 默认按“推荐优先 + 发布时间倒序”排序。
    - 状态过滤：普通用户仅见 `ACTIVE`（已发布），管理员可见所有状态。
- **详情查看**：展示完整内容、作者信息、发布时间、浏览量（预留）、点赞数。
- **点赞**：
    - 登录用户可点赞/取消点赞。
    - 自动推荐规则：点赞数达到 20 且未被推荐时，系统自动标记为推荐（代码逻辑已预埋）。
- **举报**：
    - 用户可举报违规帖子，填写举报原因。
    - 举报信息存入 `content_reports` 表，供管理员后台处理（目前仅实现提交接口）。

### 2.3 审核与推荐（管理员权限）
- **审核**：
    - 管理员在“知识审核”页面查看待审核帖子。
    - 操作：通过（状态变更为 `ACTIVE`）、驳回（状态变更为 `REJECTED`，需填写原因）。
- **推荐**：
    - 管理员可手动标记/取消标记“推荐”状态。
    - 推荐帖子在列表中置顶展示，并带有“推荐”标签。

## 3. 权限矩阵

| 角色 | 查看列表/详情 | 发布帖子 | 编辑/删除己方帖子 | 点赞/举报 | 审核帖子 | 推荐帖子 | 删除任意帖子 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **普通用户** | ✅ (仅已发布) | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| **管理员** | ✅ (所有状态) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

## 4. 数据库设计摘要
- **knowledge_posts**: 存储帖子核心信息（标题、内容、作者、状态、点赞数、是否推荐）。
- **post_likes**: 存储点赞记录（用户ID + 帖子ID），防止重复点赞。
- **content_reports**: 存储举报记录。

## 5. 测试用例概要

### 5.1 发布流程
1. **正常发布**：填写标题、内容，提交成功 -> 状态为待审核 -> 列表暂不可见（用户视角）。
2. **校验**：不填标题或内容 -> 提示必填错误。

### 5.2 审核流程（管理员）
1. **通过**：在审核页点击通过 -> 帖子状态变更为已发布 -> 用户列表可见。
2. **驳回**：点击驳回并填写原因 -> 帖子状态变更为已驳回 -> 用户在个人中心可见（需配合个人中心模块）。

### 5.3 互动流程
1. **点赞**：点击点赞 -> 图标变红，数字+1 -> 再次点击 -> 图标变灰，数字-1。
2. **自动推荐**：模拟点赞数累加至 20 -> 帖子 `is_featured` 自动变为 1 -> 列表置顶。
3. **举报**：点击举报 -> 填写原因提交 -> 提示成功。

### 5.4 权限边界
1. **编辑限制**：尝试编辑已发布帖子 -> 后端拦截并报错。
2. **删除限制**：尝试删除他人帖子（非管理员） -> 后端拦截并报错。

## 6. 整合说明
- **前端路由**：
    - 用户端入口：`/knowledge` -> `views/knowledge/index.vue`
    - 管理员审核入口：`/knowledge/audit` -> `views/knowledge/audit.vue`
- **状态管理**：
    - `userStore` 已更新，增加了 `userId` 字段，用于判断当前用户是否为帖子作者。
- **后端接口**：
    - 统一路径 `/knowledge/post/*`。
    - 集成 `Sa-Token` 进行鉴权。
