# 校园植物认养系统 - 认养申请模块开发文档

## 1. 模块概述
本模块包含植物查询页的认养申请入口及认养申请表单页。实现普通用户查询待认养植物，并发起认养申请的完整流程。

## 2. 功能详细说明

### 2.1 植物查询页 (Plant Query Page)
- **页面路径**: `/system/plant`
- **核心功能**:
    - **认养申请按钮**:
        - **显示条件**: 仅对角色为 `USER` (普通用户) 且植物状态为 `AVAILABLE` (待认养) 的记录显示。
        - **点击交互**: 
            1. 调用后端 `/adoption/status` 接口检查用户认养资格。
            2. **校验通过**: 跳转至 `/adoption/apply?plantId={id}`。
            3. **校验失败**: 弹出警告提示框，显示后端返回的拒绝原因（如“您已认养植物”或“有待审核申请”）。

### 2.2 认养申请页 (Adoption Application Page)
- **页面路径**: `/adoption/apply`
- **核心功能**:
    - **自动填充**:
        - 页面加载时根据 URL 参数 `plantId` 调用 `/plant/{id}` 接口获取植物信息。
        - 自动填充植物编号、名称、品种、科属、位置、难度、周期等字段。
        - 字段设为只读，禁止用户修改。
    - **无参数拦截**:
        - 若直接访问该页面且未携带 `plantId`，显示警告页，提示返回查询页，并隐藏表单。
    - **表单提交**:
        - **手动填写字段**: 认养周期（下拉）、联系方式（正则校验）、认养承诺（>=50字）。
        - **提交处理**: 调用 `/adoption/submit` 接口提交。
        - **提交反馈**: 成功后弹出确认框，可选择跳转至“我的申请”或“返回查询页”。

## 3. 接口规范

### 3.1 检查认养状态
- **URL**: `GET /adoption/status`
- **响应**:
  ```json
  {
    "code": 200,
    "data": {
      "canAdopt": true/false,
      "message": "可以申请" // 或拒绝原因
    }
  }
  ```

### 3.2 提交认养申请
- **URL**: `POST /adoption/submit`
- **请求体**:
  ```json
  {
    "plantId": 1001,
    "adoptionPeriodMonths": 6,
    "careExperience": "我承诺...",
    "contactPhone": "13800138000"
  }
  ```

## 4. 数据库设计
使用现有 `adoption_applications` 表，字段映射如下：
- `user_id`: 当前登录用户 ID
- `plant_id`: 申请植物 ID
- `adoption_period_months`: 认养周期
- `care_experience`: 存储认养承诺内容
- `status`: 默认为 `PENDING`

## 5. 测试用例

| ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- |
| TC01 | 普通用户正常申请 | 用户无认养记录 | 1. 在查询页点击“认养申请”<br>2. 跳转至申请页，确认植物信息填充正确<br>3. 填写表单提交 | 1. 跳转成功<br>2. 提交成功提示<br>3. 数据库新增 PENDING 记录 |
| TC02 | 已认养用户申请 | 用户已有 ACTIVE 记录 | 1. 在查询页点击“认养申请” | 1. 弹出警告“您已认养植物”<br>2. 不发生跳转 |
| TC03 | 重复申请拦截 | 用户已有 PENDING 申请 | 1. 在查询页点击“认养申请” | 1. 弹出警告“您有正在审核中的申请”<br>2. 不发生跳转 |
| TC04 | 管理员视角 | 登录管理员账号 | 1. 查看植物查询页 | 1. “认养申请”按钮不可见 |
| TC05 | 直接访问拦截 | 无 | 1. 浏览器直接输入 `/adoption/apply` | 1. 页面显示“请先选择植物”提示<br>2. 表单不显示 |

## 6. 整合说明
- **前端路由**: 需确保 `/adoption/apply` 路由已配置且指向 `src/views/adoption/apply.vue`。
- **权限控制**: 后端接口已加 `@SaCheckLogin`，前端按钮已加角色判断。
