# 植物系统升级改造设计文档

## 1. 功能概述

### 1.1 前端植物查询展示优化

- **界面重设计**：采用现代化卡片式布局，提升视觉体验
- **智能搜索**：支持多条件组合筛选（名称、类型、状态、位置等）
- **响应式布局**：完美适配桌面端、平板、手机等各种设备
- **增强功能**：植物图片轮播预览、详细信息抽屉展示、收藏功能

### 1.2 权限管理功能

- **分级权限控制**：基于角色的精细化权限管理
- **操作审计**：完整的操作日志记录和追踪
- **安全验证**：前后端双重权限校验机制

## 2. 权限设计矩阵

| 功能模块     | 普通用户 | 植物发布者         | 管理员 | 未登录用户 |
| ------------ | -------- | ------------------ | ------ | ---------- |
| 查看植物信息 | ✅       | ✅                 | ✅     | ✅         |
| 添加植物     | ✅       | ✅                 | ✅     | ❌         |
| 编辑植物     | ❌       | ✅（仅自己的）     | ✅     | ❌         |
| 删除植物     | ❌       | ✅（仅自己的）     | ✅     | ❌         |
| 审核认养申请 | ❌       | ✅（仅自己的植物） | ✅     | ❌         |
| 管理所有植物 | ❌       | ❌                 | ✅     | ❌         |
| 查看操作日志 | ❌       | ❌                 | ✅     | ❌         |

### 权限规则说明

1. **植物发布者**：对自己发布的植物拥有完全管理权限
2. **审核机制**：认养申请只能由对应植物的发布者或管理员审核
3. **权限继承**：管理员拥有所有权限，包括越权操作
4. **安全策略**：所有权限操作需在前端展示控制和后端验证

## 3. API接口设计

### 3.1 植物查询相关接口

#### 获取植物列表（支持搜索筛选）

```
GET /api/plants?page=1&limit=20&search=关键字&type=类型&status=状态&sort=创建时间
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | number | 否 | 页码，默认1 |
| limit | number | 否 | 每页数量，默认20 |
| search | string | 否 | 搜索关键字（名称、描述） |
| type | string | 否 | 植物类型筛选 |
| status | string | 否 | 植物状态筛选 |
| sort | string | 否 | 排序字段 |

**响应数据：**

```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": "plant_123",
        "name": "绿萝",
        "type": "观叶植物",
        "status": "可认养",
        "images": ["url1", "url2"],
        "description": "健康的绿萝，需要定期浇水",
        "location": "教学楼A座",
        "creator": {
          "id": "user_456",
          "name": "张老师"
        },
        "created_at": "2024-01-15T10:00:00Z",
        "can_edit": false,
        "can_delete": false,
        "can_audit": false
      }
    ],
    "total": 100,
    "page": 1,
    "limit": 20
  }
}
```

#### 获取植物详情

```
GET /api/plants/:id
```

### 3.2 植物管理接口

#### 添加植物

```
POST /api/plants
```

**请求体：**

```json
{
  "name": "绿萝",
  "type": "观叶植物",
  "description": "健康的绿萝植物",
  "location": "教学楼A座201",
  "care_instructions": "每周浇水2次，避免阳光直射",
  "images": ["image_url_1", "image_url_2"]
}
```

#### 更新植物信息

```
PUT /api/plants/:id
```

**权限验证：**

- 用户只能更新自己发布的植物
- 管理员可以更新任何植物

#### 删除植物

```
DELETE /api/plants/:id
```

**权限验证：**

- 用户只能删除自己发布的植物
- 管理员可以删除任何植物

### 3.3 认养审核接口

#### 获取认养申请列表

```
GET /api/plants/:id/adoption-applications
```

#### 审核认养申请

```
POST /api/adoption-applications/:id/audit
```

**请求体：**

```json
{
  "action": "approve" | "reject",
  "reason": "审核说明（拒绝时必填）"
}
```

**权限验证：**

- 仅植物发布者和管理员可以审核
- 需要验证申请对应的植物归属

### 3.4 操作日志接口

#### 获取操作日志

```
GET /api/logs?target_type=plant&target_id=123&action=update
```

**权限验证：**

- 普通用户只能查看自己的操作日志
- 管理员可以查看所有日志

## 4. 前端设计方案

### 4.1 植物搜索页面

#### 布局设计

- **顶部搜索栏**：关键词搜索 + 高级筛选按钮
- **侧边筛选栏**：植物类型、状态、位置等筛选条件
- **主内容区**：卡片式植物展示网格
- **底部分页**：支持页码跳转和每页数量选择

#### 交互设计

- **卡片悬停**：显示快速操作按钮（收藏、预览）
- **点击查看**：侧边抽屉展示详细信息
- **响应式适配**：
  - 桌面端：4列网格布局
  - 平板端：2-3列网格布局
  - 手机端：单列列表布局

#### 视觉设计

- **色彩方案**：
  - 主色调：#52c41a（植物绿）
  - 辅助色：#f6ffed（浅绿背景）
  - 强调色：#fa8c16（橙色用于重要操作）
- **卡片样式**：圆角设计 + 轻微阴影 + 悬停上浮效果
- **图片展示**：统一比例裁剪 + 懒加载优化

### 4.2 植物详情展示

#### 展示方式

- **模态框/抽屉**：根据设备尺寸自动选择
- **图片轮播**：支持多张植物图片展示
- **信息分层**：
  - 基本信息（名称、类型、状态）
  - 详细描述（养护说明、位置信息）
  - 操作记录（创建时间、更新时间）

#### 权限相关展示

- **编辑按钮**：仅对植物发布者和管理员显示
- **删除按钮**：添加二次确认对话框
- **审核面板**：认养申请列表和审核操作

### 4.3 用户个人中心

#### 我的植物管理

- **植物列表**：展示用户发布的所有植物
- **状态统计**：可认养、已认养、待审核数量
- **快速操作**：编辑、删除、查看认养申请

#### 认养审核管理

- **待审核列表**：需要审核的认养申请
- **审核历史**：已处理的申请记录
- **批量操作**：支持批量通过/拒绝申请

### 4.4 响应式设计细节

#### 断点设置

- 手机端：< 768px
- 平板端：768px - 1024px
- 桌面端：> 1024px

#### 适配策略

- **图片适配**：根据屏幕尺寸加载不同分辨率图片
- **字体大小**：使用rem单位，确保可缩放性
- **触控优化**：按钮最小44px点击区域
- **性能优化**：移动端减少动画效果，优先加载关键内容

## 5. 技术实现要点

### 5.1 前端技术栈

- **框架选择**：React 18 + TypeScript
- **状态管理**：React Context + useReducer
- **UI组件库**：Ant Design + 自定义组件
- **样式方案**：Tailwind CSS + CSS Modules
- **构建工具**：Vite

### 5.2 权限验证实现

- **路由守卫**：基于角色的路由访问控制
- **组件级权限**：权限高阶组件封装
- **API请求拦截**：统一添加权限token
- **前端缓存**：权限信息本地缓存策略

### 5.3 性能优化

- **虚拟滚动**：大量数据列表优化
- **图片懒加载**：Intersection Observer实现
- **数据缓存**：React Query进行接口数据缓存
- **代码分割**：路由级别的懒加载

## 6. 测试验收标准

### 6.1 功能测试

- ✅ 所有页面功能正常可用
- ✅ 权限控制准确无误
- ✅ 数据展示完整正确
- ✅ 用户操作流程顺畅

### 6.2 兼容性测试

- ✅ Chrome、Firefox、Safari、Edge浏览器兼容
- ✅ iOS、Android移动端适配
- ✅ 不同分辨率屏幕显示正常

### 6.3 性能测试

- ✅ 页面加载时间 < 2秒
- ✅ 查询响应时间 < 2秒
- ✅ 图片加载优化到位
- ✅ 内存使用合理

### 6.4 安全测试

- ✅ 无SQL注入漏洞
- ✅ 无XSS攻击风险
- ✅ 权限验证完善
- ✅ 敏感信息保护

## 7. 部署说明

### 7.1 环境要求

- Node.js >= 16.0.0
- 现代浏览器支持
- HTTPS协议（生产环境）

### 7.2 部署步骤

1. 安装依赖：`npm install`
2. 环境配置：复制`.env.example`为`.env`并配置
3. 构建项目：`npm run build`
4. 部署到服务器或CDN

### 7.3 监控配置

- 前端错误监控：Sentry集成
- 性能监控：Web Vitals指标收集
- 用户行为分析：埋点数据收集
