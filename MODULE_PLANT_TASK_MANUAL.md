# 植物管理与任务模板模块开发文档

## 1. 模块概述
本模块包含「植物管理」与「养护任务模板管理」两大核心功能，旨在实现校园植物的全生命周期管理以及养护任务的标准化生成。

## 2. 功能清单

### 2.1 植物管理模块
- **基础信息管理**：新增、编辑、删除、详情查看、分页查询。
- **条件筛选**：支持按品种、区域、养护难度、认养状态筛选。
- **批量操作**：支持 Excel 导入/导出。
- **状态管理**：待认养/已认养状态切换。
- **数据关联**：查看植物关联的认养申请、养护任务（在详情页整合）。

### 2.2 任务模板管理模块
- **模板管理**：新增、编辑、删除、详情查看、分页查询。
- **模板复用**：支持复制现有模板快速创建。
- **状态管理**：启用/禁用模板。
- **自动关联**：基于「植物品种」自动关联植物，修改模板后自动同步未执行任务（逻辑已预留）。

## 3. 业务规则与约束

### 3.1 植物管理
- **唯一性**：植物编号唯一；同一位置下的同一品种植物唯一。
- **删除约束**：已关联认养申请或养护任务的植物禁止删除。
- **必填项**：编号、名称、品种、位置、养护难度。

### 3.2 任务模板
- **唯一性**：同一植物品种 + 任务类型组合唯一。
- **关联约束**：模板只能关联已存在的植物品种。
- **删除约束**：已关联未完成（PENDING）养护任务的模板禁止删除。

## 4. 权限矩阵

| 角色 | 植物列表 | 植物详情 | 植物新增/编辑/删除 | 植物导入/导出 | 模板列表 | 模板新增/编辑/删除 | 模板状态调整 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **普通用户** | ✅ | ✅ | ❌ | ❌ | ✅(仅启用) | ❌ | ❌ |
| **养护员** | ✅ | ✅ | ❌ | ❌ | ✅(仅启用) | ❌ | ❌ |
| **管理员** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

## 5. 数据关联逻辑
1. **植物 - 认养**：`adoption_applications` 表通过 `plant_id` 关联 `plants` 表。
2. **植物 - 任务**：`care_tasks` 表通过 `plant_id` 关联 `plants` 表。
3. **模板 - 任务**：`care_tasks` 表通过 `task_template_id` 关联 `task_templates` 表。
4. **模板 - 植物**：通过 `plant_species` (String) 字段进行逻辑关联，非外键强关联，支持灵活匹配。

## 6. 测试用例概要
- **植物新增**：正常新增；重复编号报错；重复位置品种报错。
- **植物删除**：无关联删除成功；有关联删除报错。
- **模板新增**：正常新增；不存在的品种报错；重复类型报错。
- **模板删除**：无关联任务删除成功；有未完成任务删除报错。

## 7. 整合说明
- **前端整合**：
  - 植物管理入口：`/system/plant`
  - 模板管理入口：`/system/task-template`
  - 详情页已预留关联信息展示区域。
- **后端整合**：
  - `PlantService` 已注入 `AdoptionApplicationMapper` 和 `CareTaskMapper` 用于关联检查。
  - `TaskTemplateService` 已注入 `PlantMapper` 用于品种校验。
